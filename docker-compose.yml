version: '3.8'
services:
  web:
    build: .
    restart: always
    ports:
    - "3003:8888"
    # volumes:
    #   - "/srv/docker-data/DBS/jupiter:/home/jovyan"
    cap_add:
      - NET_ADMIN
    devices:
      - "/dev/net/tun"
    sysctls:
      net.ipv6.conf.all.disable_ipv6: '0'